- if controller_name != 'sessions'
  = link_to "Log in", new_session_path(resource_name), class: 'Button__login'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "SIGN UP", new_registration_path(resource_name), class: 'Button__signin'
  %br/
